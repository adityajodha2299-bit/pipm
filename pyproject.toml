[project]
name = "pipm"
version = "0.7.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "rich>=14.3.3",
    "typer>=0.24.1",
    "packaging",
    "pytest>=9.0.3",
]

[project.scripts]
pipm = "pipm.cli.__main__:app"
